{% extends 'layouts/app.twig' %}

{% block content %}
    <div class="container">
        <div class="section">
            <p class="is-size-1 has-text-weight-bold has-text-centered">
                404.
            </p>

            <p class="is-size-4 has-text-centered">
                Vamos voltar para <a href="{{ route('home') }}">home</a> e tentar novamente.
            </p>
        </div>
    </div>
{% endblock %}
